function sortNumber(a, b, c) {
  // TODO: Implement Function
  // let all = [a, b, c]
  // let first = Math.min(...all)
  // let last = Math.max(...all)
  // let firstIndex = all.indexOf(first)
  // all.splice(firstIndex, 1)
  // let lastIndex = all.indexOf(last)
  // all.splice(lastIndex, 1)
  // return [first, ...all, last]
  return [a, b, c].sort()
}

module.exports = sortNumber;